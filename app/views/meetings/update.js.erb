$('.header_flash_message').html("<%= j render 'layouts/flash_messages' %>");
<% if @meeting.errors.blank? %>
  $("#meetings-tab-body").html("<%= escape_javascript(render partial: 'venues/meetings_list', locals: { meetings: @meeting.meetingable.meetings, venue: @meeting.meetingable }) %>");
  $("#bottom_modal").modal('hide');
<% end %>
