$('#right-modal-content').html("<%= j render partial: 'form', locals: {event: @event, venue: @venue} %>")
$("#right_modal").modal('show');
$('.modal-content').scrollTop(0);
$('form').parsley();
$('.event_brands_fields').find('.nested-fields').first().find('span').addClass('d-none');
