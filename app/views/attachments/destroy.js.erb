$("#collapseAttach").html("<%= j render partial: 'venues/attachment_list', locals: {venue: @venue, attachable: @attachable} %>")
$('.attachment-tab-div').html("<%= j render partial: 'venues/attachment_list', locals: {venue: @venue, attachable: @attachable} %>")
$(".total-count").text("<%= @attachable.attachments.count%>")
$('.header_flash_message').html("<%= j render 'layouts/flash_messages' %>");
$("div.attachment-<%=@attachment.id%>").remove()
