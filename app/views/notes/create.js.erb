$('.header_flash_message').html("<%= j render 'layouts/flash_messages' %>");
$('.notes-list').html('<%= j render(partial: "#{@notable.class.name.pluralize.downcase}/notes_list", locals: {notes: @notable.notes, notable: @notable}) %>')
$('#bottom_modal').modal('hide');
